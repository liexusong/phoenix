// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("phoenix", "for phoenix support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("phoenix", "enable phoenix support", "no");

if (PHP_PHOENIX != "no") {
	EXTENSION("phoenix", "phoenix.c");
}

